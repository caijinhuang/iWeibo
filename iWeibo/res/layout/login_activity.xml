<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/page_welcom"
    android:gravity="center|center_horizontal"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="110dp"
        android:layout_height="110dp"
        android:background="@drawable/dialog_bg"
        android:gravity="center|center_horizontal"
        android:orientation="vertical" >

        <ImageView
            android:id="@+id/head"
            android:layout_width="90dp"
            android:layout_height="90dp" />
    </LinearLayout>

    <TextView
        android:id="@+id/screen_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="10dp"
        android:textColor="#00ff00" />

    <LinearLayout
        android:layout_width="190dp"
        android:layout_height="40dp"
        android:background="@drawable/account_selete_bg"
        android:gravity="center|center_horizontal"
        android:orientation="vertical" >

            <Spinner
                android:id="@+id/screen_name_list"
                android:layout_width="95dp"
                android:layout_height="30dp"
                android:layout_gravity="left"
                android:theme="@style/spinner_theme"
                android:background="#00000000"
                android:padding="3dp" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:gravity="bottom|center_horizontal"
        android:orientation="horizontal" >

        <Button
            android:id="@+id/but_login"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bt_select_login"
            android:text="@string/login_go"
            android:textColor="#ffffff" />

        <Button
            android:id="@+id/but_oauth"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bt_select_login"
            android:text="@string/login_cancel"
            android:textColor="#ffffff" />
    </LinearLayout>

</LinearLayout>